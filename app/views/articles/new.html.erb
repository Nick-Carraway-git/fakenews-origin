<%= form_with(model: @article, local: true) do |f| %>
  <div class="field">
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
